# Requirements for Pose Detection OSC
# Install with: pip install -r requirements.txt

# Note: MediaPipe requires Python 3.8-3.12 (not 3.13+ yet)
# If you have Python 3.13, please install Python 3.12 or use pyenv

opencv-python>=4.8.0
mediapipe>=0.8.0,<0.11.0
python-osc>=1.8.0
numpy>=1.24.0,<2.0.0

# Alternative packages if MediaPipe fails
# ultralytics  # YOLO-based pose detection alternative
# torch torchvision  # PyTorch-based alternatives